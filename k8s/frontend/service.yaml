apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: medical
  labels:
    app: frontend
    component: web
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
