apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: medical
  labels:
    app: patient-service
    component: backend
spec:
  selector:
    app: patient-service
  ports:
    - port: 3000
      targetPort: 3000
