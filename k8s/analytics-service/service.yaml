apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: medical
  labels:
    app: analytics-service
    component: backend
spec:
  selector:
    app: analytics-service
  ports:
    - port: 3000
      targetPort: 3000
